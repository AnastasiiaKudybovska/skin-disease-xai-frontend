import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    // primary: { main: '#1976d2' },
    // secondary: { main: '#dc004e' },
    text: {
      primary: '#1F222A',
      white: '#F7F8FF'
    }
  },
  // typography: {
  //   fontFamily: '"Inter", "Frank Ruhl Libre", "Roboto", "Helvetica", "Arial", sans-serif',
  // },
});

export default theme;